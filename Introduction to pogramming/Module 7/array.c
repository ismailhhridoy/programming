#include <stdio.h>

int main() {
 
    int ar[3]; // array index start from 0
    ar[0] = 1000;
    ar[1] = 2000;
    ar[2] = 3000;
    // scanf("%d %d %d", &ar[0], &ar[1], &ar[2]);
    printf("%d %d %d", ar[0], ar[1], ar[2]);

    return 0;
}